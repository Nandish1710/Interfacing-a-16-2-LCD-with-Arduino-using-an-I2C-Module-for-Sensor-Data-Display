# Interfacing a 16×2 LCD with Arduino UNO R4 WiFi Using an I2C Module

## Aim
To interface a 16×2 LCD with Arduino UNO R4 WiFi using an I2C module and display sensor data on the LCD.

## Hardware / Software Tools Required
* Arduino UNO R4 WiFi
* 16×2 LCD Display
* I2C LCD Module
* USB Cable
* Jumper Wires
* Breadboard
* Arduino IDE
* Wokwi Online Simulator

## Circuit Diagram
![Circuit Diagram](https://github.com/user-attachments/assets/b46ab1c7-beaa-4044-a6a6-de27bae174ee)

## Circuit Connections

| I2C LCD Pin | Arduino UNO R4 WiFi |
|-------------|----------------------|
| VCC         | 5V                   |
| GND         | GND                  |
| SDA         | SDA                  |
| SCL         | SCL                  |

## Working Principle
The 16×2 LCD is interfaced with the Arduino UNO R4 WiFi using an I2C module to reduce the required wiring connections.

Arduino UNO R4 WiFi
↓
I2C Interface
↓
16×2 LCD
↓
Display Data

## Procedure

1. **Create the Wokwi Project**
   * Open the Wokwi online simulator.
   * Create a new project using Arduino UNO R4 WiFi.
   * Add a 16×2 LCD with an I2C interface.
   * Connect the LCD to the Arduino UNO R4 WiFi and verify connections.

2. **Connect the I2C LCD**
   * Connect **VCC** to 5V.
   * Connect **GND** to GND.
   * Connect **SDA** to Arduino SDA.
   * Connect **SCL** to Arduino SCL.

3. **Configure the Program**
   * Open the Arduino IDE or Wokwi code editor.
   * Include `<Wire.h>` and `<LiquidCrystal_I2C.h>` libraries.
   * Create the LCD object with I2C address `0x27`.
   * Initialize the LCD and turn ON the backlight.

4. **Run the Program**
   * Start the Wokwi simulation.
   * Observe the LCD backlight turning on and displaying text.

## Program

```cpp
#include <Wire.h>
#include <LiquidCrystal_I2C.h>

LiquidCrystal_I2C lcd(0x27, 16, 2);

void setup()
{
  lcd.init();
  lcd.backlight();

  lcd.setCursor(3, 0);
  lcd.print("GAME OVER");

  lcd.setCursor(2, 1);
  lcd.print("Score: 25");
}

void loop()
{
}
```
## Output

<img width="1599" height="1053" alt="image" src="https://github.com/user-attachments/assets/0f249975-65b8-4ecd-ba55-f7e76d014b0c" />


## Result.
The 16×2 LCD was successfully interfaced with the Arduino UNO R4 WiFi using an I2C module, and text was correctly displayed on the screen.
